# Copy to .env and fill in. Never commit .env.
# Share real keys via password manager or AirDrop — never in chat or a commit.

SARVAM_API_KEY=
OPENAI_API_KEY=

# Optional overrides
OPENAI_MODEL=gpt-5.6
DRISHTI_PROFILE=dev
